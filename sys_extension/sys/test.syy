// SysY+ 测试文件
// 测试语法高亮、错误检测、代码补全等功能

int main() {
    // 测试变量声明
    int x = 10;
    const int y = 20;
    float z = 3.14;
    char c = 'A';
    
    // 测试数组
    int arr[5] = {1, 2, 3, 4, 5};
    
    // 测试函数调用
    int result = add(x, y);
    
    // 测试控制流
    if (x > 0) {
        while (x > 0) {
            x = x - 1;
        }
    }
    
    // 测试结构体
    struct Point {
        int x;
        int y;
    };
    
    return 0;
}

// 测试函数定义
int add(int a, int b) {
    return a + b;
}

// 以下是一些故意的错误，用于测试错误检测：

// 1. 使用关键字作为变量名
// int if = 5;  // 取消注释会报错

// 2. 未定义的变量
// int result2 = unknown_var;  // 取消注释会报错

// 3. 数组越界
// int value = arr[10];  // 取消注释会报错

// 4. 给常量赋值
// y = 30;  // 取消注释会报错

// 5. 中文字符
// int 变量 = 10;  // 取消注释会报错 